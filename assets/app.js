const navToggle=document.querySelector('.nav-toggle');const navMenu=document.querySelector('#primary-menu');const toast=document.querySelector('#toast');const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();function showToast(message){if(!toast)return;toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}if(navToggle&&navMenu){navToggle.addEventListener('click',()=>{const open=navMenu.classList.toggle('is-open');navToggle.setAttribute('aria-expanded',String(open))});navMenu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{navMenu.classList.remove('is-open');navToggle.setAttribute('aria-expanded','false')}))}document.querySelectorAll('form[data-form]').forEach(form=>{form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const formType=form.dataset.form||'form';const data=Object.fromEntries(new FormData(form).entries());console.info(`Zeza demo ${formType} submission`,data);localStorage.setItem(`zeza-${formType}-last-submit`,JSON.stringify({data,submittedAt:new Date().toISOString()}));showToast('Demo submission saved in this browser');form.reset()})});const resourceSearch=document.querySelector('#resource-search');if(resourceSearch){resourceSearch.addEventListener('input',()=>{const query=resourceSearch.value.trim().toLowerCase();document.querySelectorAll('.resource-card').forEach(card=>{const haystack=`${card.textContent} ${card.dataset.search||''}`.toLowerCase();card.style.display=!query||haystack.includes(query)?'block':'none'})})}const cookieBanner=document.querySelector('#cookie-banner');const cookieButton=document.querySelector('#accept-cookies');if(cookieBanner&&localStorage.getItem('zeza-cookie-consent')==='accepted')cookieBanner.classList.add('is-hidden');if(cookieButton){cookieButton.addEventListener('click',()=>{localStorage.setItem('zeza-cookie-consent','accepted');cookieBanner?.classList.add('is-hidden');showToast('Cookie preference saved')})}document.querySelectorAll('.product-card button').forEach(button=>button.addEventListener('click',()=>showToast('Demo product action. Connect payments during production.')));
